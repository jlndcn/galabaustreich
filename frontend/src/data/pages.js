// Structured page copy – editable via Decap CMS (src/content/*.json).

import about from "@/content/about.json";
import team from "@/content/team.json";

export const aboutContent = about;
export const teamContent = team;
