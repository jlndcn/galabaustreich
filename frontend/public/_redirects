# Nur Admin-Trailing-Slash. Kein /* → /index.html:
# Diese Catch-all-Regel überschreibt auf Cloudflare Pages IMMER statische Dateien
# (auch googled*.html) und bricht die Search-Console-Verifikation.
# SPA-Routing übernimmt Pages automatisch, solange kein top-level 404.html existiert.
/admin   /admin/   301
